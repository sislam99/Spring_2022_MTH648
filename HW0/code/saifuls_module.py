

import numpy as np

def average_of_two_numbers(x,y):
	"""
    A function calculating the average of two numbers.

    Args:
        x (float): Some number.
        y (float): Some number.

    Returns:
        float: Returns :math:`average=(x+y)/2`
    """
    
	avg=(x+y)/2

	return avg

def sum_of_three_numbers(x,y,z):
	"""
    Second function to find the summation of three numbers.

    Args:
        x (float): Some number.
        y (float): Some number.
        z (float): Some number.

    Returns:
        float: Returns :math:`s=x+y+z`
    """
    
    

	return x+y+z